{{#if editing}}
  {{configuration/pipeline-card/edit pipeline=pipeline editable=editableAction}}
  {{else}}
  {{configuration/pipeline-card/show pipeline=pipeline select=select editable=editableAction}}
{{/if}}
